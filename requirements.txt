# ---- Core web framework
Flask>=3.0,<4.0
Jinja2>=3.1,<4.0

# ---- Database & ORM
SQLAlchemy>=2.0,<3.0
Flask-SQLAlchemy>=3.1,<4.0

# ---- Migrations (flask db migrate/upgrade)
Flask-Migrate>=4.0,<5.0
alembic>=1.13,<2.0

# ---- Forms & CSRF
Flask-WTF>=1.2,<2.0
email-validator>=2.1,<3.0

# ---- Environment variables (.env for local/dev)
python-dotenv>=1.0,<2.0

# ---- Production WSGI server (needed on Azure Linux)
gunicorn>=21.2,<22.0

# ---- Optional data tools (remove if not used)
# pandas>=2.0,<3.0
