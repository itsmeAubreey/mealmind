# ---- Core web framework
Flask>=3.0,<4.0

# ---- Database & ORM
SQLAlchemy>=2.0,<3.0
Flask-SQLAlchemy>=3.1,<4.0

# ---- Env vars (so local .env works; Azure ignores it)
python-dotenv>=1.0,<2.0

# ---- Security / tokens (used for password reset)
itsdangerous>=2.2,<3.0

# ---- MFA (we kept your TOTP routes)
pyotp>=2.9.0,<3.0

# ---- Optional OAuth (so Auth0 part in app.py won't crash if you set the env vars)
authlib>=1.3,<2.0

# ---- Production server for Azure Linux
gunicorn>=21.2,<23.0
